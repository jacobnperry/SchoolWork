#include <iostream>
#include <vector>
using namespace std;

void SortVector(vector<int>& myVec){

    int i;
    for (i = 0; i < myVec.size(); i++){
        if(myVec(i)<)
    }

}

int main() {
   int vectorSize;
   cin >> vectorSize;
   vector<int> myVec(vectorSize);
   
   SortVector(myVec);/* Type your code here */

   return 0;
}